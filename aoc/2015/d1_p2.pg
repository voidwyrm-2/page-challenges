/~str (strings) import def

/n 0 def
/pos 1 def

/inc {dup load 1 add def} def

(d1.txt) readf
~str.chars

{
  dup

  (\() eq
  {/n inc}
  if

  (\)) eq
  {/n n 1 sub def}
  if

  n 0 lt
  {pos = quit}
  if

  /pos inc
} forall
